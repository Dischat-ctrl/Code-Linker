## Packages
framer-motion | Complex animations for tab switching and history drawer
date-fns | Formatting timestamps for history
clsx | Utility for conditional class names
tailwind-merge | Utility for merging tailwind classes

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  mono: ["'JetBrains Mono'", "monospace"],
  sans: ["'Inter'", "sans-serif"],
  display: ["'Oxanium'", "display"],
}
This app uses a "Browser in Browser" architecture.
Authentication uses Replit Auth via /api/login.
